import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class SantaSlay here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class SantaSlay extends Actor
{
    /**
     * Act - do whatever the SantaSlay wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
        public SantaSlay()
    {
        GreenfootImage image = getImage();
        image.scale(400,200);
        setImage(image);
    }
}
